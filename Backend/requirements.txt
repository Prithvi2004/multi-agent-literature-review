# Backend requirements for Advanced Multi-Agent RAG System
# NOTE (Windows users): `faiss-cpu` is not reliably installable via pip on
# Windows. Use conda to install FAISS on Windows (see steps below) and keep
# the pip requirement conditional so `pip install -r` does not attempt FAISS on
# Windows.

# Pin critical dependencies to avoid build issues on deployment platforms
jiter>=0.9.0  # Ensure pre-built wheels are used (avoid Rust compilation)
openai>=1.40.0,<2.0.0  # Pin openai to avoid jiter version conflicts

# Ollama Python SDK for cloud API
ollama>=0.4.0

# Core Agent Framework
crewai>=0.11.0
crewai-tools

# LLM & Embeddings
langchain>=0.1.0
langchain-ollama
langchain-community
sentence-transformers>=2.2.0

# Advanced RAG Components
# rank-bm25>=0.2.2  # Sparse retrieval (BM25)
# ragas>=0.1.0  # RAG evaluation metrics
voyageai  # Advanced embeddings (optional)

# Document Processing
pypdf
beautifulsoup4
unstructured[local-inference]
python-magic-bin; platform_system == "Windows"  # File type detection

# Search & Retrieval
arxiv
requests
urllib3

# Data Processing & ML
numpy>=1.24.0
scipy
scikit-learn
pandas  # For evaluation metrics

# Utilities
tqdm
python-dotenv
aiohttp  # Async HTTP requests
asyncio  # Async processing

# Logging & Monitoring
colorlog  # Enhanced logging
rich  # Beautiful terminal output

# FAISS is best installed via conda on Windows
faiss-cpu; platform_system != "Windows"

# Web Framework for API
Flask>=3.0.0
Flask-CORS>=4.0.0

# Production deployment
gunicorn>=21.0.0